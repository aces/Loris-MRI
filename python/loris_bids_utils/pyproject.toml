[project]
name = "loris-bids-utils"
version = "29.0.0"
description = "The LORIS BIDS utilities library"
readme = "README.md"
license = "GPL-3.0-or-later"
requires-python = ">= 3.11"
dependencies = [
    "loris-utils",
    "pybids",
]

[project.scripts]
copy-bids-ctf-ds = "loris_bids_utils.scripts.copy_bids_ctf_ds:main"
copy-ctf-ds      = "loris_bids_utils.scripts.copy_ctf_ds:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/loris_bids_utils"]

[tool.ruff]
extend = "../../pyproject.toml"
src = ["src"]
